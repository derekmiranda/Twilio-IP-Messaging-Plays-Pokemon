{
  "name": "twilio-ip-messaging-plays-pokemon",
  "version": "0.1.0",
  "description": "Recreation of Twitch Plays Pokemon using Twilio. NOTE: Server also requires Cairo (https://www.cairographics.org/download/) for node-canvas dependency.",
  "dependencies": {
    "blob": "0.0.1",
    "debug": "0.7.4",
    "dotenv": "^4.0.0",
    "express": "^3.21.2",
    "jquery": "^1.11.0",
    "mustache-express": "^1.0.1",
    "redis": "0.10.1",
    "socket.io-client": "^1.3.7",
    "twilio": "^3.0.0-edge"
  },
  "devDependencies": {
    "browserify": "12.0.1",
    "browserify-middleware": "^7.0.0"
  },
  "scripts": {
    "deps": "npm install && cd weplay && npm install && cd ../weplay-emulator && npm install",
    "start": "node index"
  }
}
